# Frontend environment variables (client-exposed)
# Copy this file to `.env.local` and edit values for your environment.
# Do NOT commit secrets to git. This file is intended for local development only.

# Base URL for the backend API. If empty, the frontend will use relative paths (/api/...)
# Example: http://localhost:8000 or https://api.example.com
NEXT_PUBLIC_API_BASE_URL=http://thelastupdates.com:8000

# Optional: Supabase (if you want to surface anon key in the frontend; often not necessary)
#NEXT_PUBLIC_SUPABASE_URL=
#NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: feature flags
#NEXT_PUBLIC_ENABLE_LOGGING=true
